server.port=8080

# CORS — allow Next.js frontend
dhruva.cors.allowed-origin=http://localhost:3000

# Path where the Excel file is stored (relative to where the JAR runs)
dhruva.excel.file-path=data/submissions.xlsx
